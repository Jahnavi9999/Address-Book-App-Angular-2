export class MockData {
         id ?: number;
         name ?: string; // ? to check null


         constructor(id: number, name: string) {
          this.id = id;
          this.name = name;

         }
       }
